import { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Events — SF Slackline',
  description: 'SF Slackline events past and present. Pride lines, Ocean Beach sessions, nightlines, highline firsts, and community gatherings.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
