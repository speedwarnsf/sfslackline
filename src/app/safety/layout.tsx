import { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Safety & Law — SF Slackline',
  description: 'SF Rec & Park slackline policy (now a 404), ISA tree protection standards, enforcement history from 2011-2018, and injury archive.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
