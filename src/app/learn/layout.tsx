import { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Learn — SF Slackline',
  description: 'Learn to slackline from beginner to highline. Video tutorials, progression path, technique tips, and tree protection basics.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
